name: "Hello World"

on:
    push:
        branches:
            - main

jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        steps:
            - name:
              run: |
                  echo "Hello World"
